<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LostAndFound" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/brand_link</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:statusBarColor">@color/sys_status_bar</item>
        <item name="android:navigationBarColor">@color/sys_nav_bar</item>
    </style>
</resources>